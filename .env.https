# Environment variables for HTTPS development
# Copy this to .env to use HTTPS URLs in development

# API URL for HTTPS development
VITE_API_URL=https://api.localhost/api

# Config Cache Timeout
VITE_CONFIG_CACHE_TIMEOUT=5000

# Server Port (backend still runs on HTTP internally)
PORT=3000

# Encryption configuration (optional)
# ENCRYPTION_KEY=your-base64-encoded-32-byte-key-here